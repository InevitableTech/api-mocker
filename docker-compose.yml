version: '3.3'

services:
  mock-api:
    build: .
    ports:
      - "${PORT}:${PORT}"
    networks:
      - "${NETWORK}"
    env_file:
      - .env.external
    volumes:
      - .:/app
      - ${API_MOCK_STATICS_DIR}:/app/static
    command: nodemon --watch . index.php
